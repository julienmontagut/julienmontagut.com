---
import BasePage from "./BasePage.astro";

export interface Props {
    title: string
}

const { title } = Astro.props;
---

<BasePage title={title}>
    <main class="container max-w-screen-xl justify-center m-auto p-4 pt-8">
        <!-- <h1 class="text-6xl font-serif font-bold ml-4 bg-gradient-to-r from-ultramarine to-rust bg-clip-text text-transparent">{title}</h1> -->
        <slot />
    </main>
</BasePage>
